name: 'projet5_dbt'
version: '1.0.0'
config-version: 2

profile: 'projet5_dbt'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  projet5_dbt:
    staging:
      materialized: view
    intermediate:
      materialized: view
    marts:
      core:
        materialized: table
